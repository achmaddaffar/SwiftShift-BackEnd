ktor {
    deployment {
        port = 8005
        port = ${?PORT}
    }
    application {
        modules = [ com.swiftshift.ApplicationKt.module ]
    }
}
jwt {
    domain = "http://0.0.0.0:8005"
    audience = "main"
    secret = "secret"
    realm = "swiftshift"
}